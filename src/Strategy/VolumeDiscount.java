package Strategy;

public class VolumeDiscount implements DiscountStrategy{
    public double calculateDiscount(double totalPrice) {
        // Реализация скидок на объем
        // ...
        return totalPrice;
    }
}
